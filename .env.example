# .env.example
#
# This file serves as a template for your actual .env file.
# To get started, copy this file to a new file named `.env` in the root directory
# of the project and fill in your actual credentials and desired configurations.
#
# IMPORTANT: Do NOT commit your actual .env file to version control!
# It contains sensitive information like API keys. The `.gitignore` file is
# already configured to ignore `.env` automatically, but always double-check.

# Google Gemini API Key
# ---------------------
# Your API key for accessing the Google Gemini API.
# You can obtain a free API key from: https://ai.google.dev/ or https://aistudio.google.com/app/apikey
GEMINI_API_KEY="YOUR_GEMINI_API_KEY_HERE"


# Optional Configuration
# ----------------------
# Uncomment and modify these variables if you wish to customize the chatbot's behavior.
# If left commented out or unset, the application will use sensible default values.

# The name of the Gemini model to use (e.g., "gemini-2.0-flash-exp", "gemini-1.5-pro").
# Defaults to "gemini-2.0-flash-exp" if not specified.
# MODEL_NAME="gemini-2.0-flash-exp"

# The temperature setting for the model (controls randomness, 0.0-1.0).
# Lower values make the output more deterministic, higher values more creative.
# Defaults to 0.7 if not specified.
# MODEL_TEMPERATURE=0.7

# Maximum allowed size for uploaded files in megabytes (MB).
# Note: Gemini File Search API has a hard limit of 100MB per file.
# Defaults to 100 MB if not specified.
# MAX_FILE_SIZE_MB=100

# Logging Configuration
# Set the desired logging level for the application.
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Defaults to INFO if not specified.
# LOG_LEVEL=INFO

# Enable or disable file logging (logs will be saved to logs/ directory)
# Options: true, false
# Defaults to true if not specified.
# FILE_LOGGING_ENABLED=true

# Maximum size of each log file in bytes before rotation
# Defaults to 10485760 (10 MB) if not specified.
# LOG_MAX_BYTES=10485760

# Number of backup log files to keep during rotation
# Defaults to 5 if not specified.
# LOG_BACKUP_COUNT=5
